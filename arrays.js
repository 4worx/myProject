/*
let songs = [
    "classic",
    "rap",
    "dubstep",
];

songs[2] = "N G" //changes a song from the list above
songs.push("hiHop"); //puts in another song with the above list
songs.pop(); //makes one item from the list dissapear

console.log(songs); //shows the list
console.log(songs.length); //shows the number of items in the list
*/

let favSites = [
    "Novels",
    "Mangas",
    "Animations"
];


favSites.push("RR", "RN");
//favSites.pop(); //deletes the last item

//favSites.shift(); //deletes an item from the top

//favSites.unshift('Novels'); //adds an item on top
//by enetering it within the bracket.

//favSites.slice(0, 3); //copies a part from the item
//then returns the copied part without making any changes 
//to the orioginal array

//favSites.splice(1); //deletes the item list after the indicated 
//number within brackets

//console.log(favSites.slice(0, 3));
console.log(favSites);